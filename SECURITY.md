# Security policy

Use GitHub private vulnerability reporting for weaknesses in the runner, evidence validation, protected inputs, signing, or candidate isolation. Never include production credentials, customer data, or unredacted agent/model content.

Compromise that lets a candidate influence its oracle, expected result, runner digest, or attestation identity is a release-integrity failure. Local runner output is never a substitute for the protected verifier identity.
